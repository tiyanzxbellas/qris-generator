{
  "name": "qris-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Generator QRIS dinamis dari gambar QRIS statis, 100% di browser (tanpa server/API)",
  "scripts": {
    "test": "node test/run.js",
    "start": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "jpeg-js": "^0.4.4",
    "pngjs": "^7.0.0"
  }
}
